os: linux
dist: xenial
language: python
python:
  - '3.8'
cache: pip
install:
  - cd /home/travis/build/elcolumbio/fastcounting/
  - pip install -r requirements.txt
  - pip install .

script:
  - flake8 ./fastcounting/
  - flake8 ./tests
  - pytest